VITE_API_URL=https://your-backend-project.vercel.app/api

# --- Pusher (real-time chat/order notifications) ---
# Same Pusher app as the backend, but only the KEY and CLUSTER are public
# (never the secret) - these two are safe to expose to the browser.
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=
